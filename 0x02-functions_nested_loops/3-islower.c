#include "main.h"
#include <stdio.h>
/**
 * main - check function for lowercase character
 *
 * Return: Always return 0
 */

int main(void)
{
	char ch;

	if (ch >= 'A' && ch <= 'Z')
	{
		putchar('i');
	}
	else
	{
		putchar('0');
	}
}
